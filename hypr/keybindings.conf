###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
$wm=Window Management
$d=[$wm]

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, X, killactive, 
bind = $mainMod, F, fullscreen, immediate
bind = $mainMod, V, togglefloating, 
bind = $mainMod SHIFT, C, exec, hyprctl reload   
bind = $mainMod, P, pin

# System

bindd = ALT_R, Control_R, toggle quickshell, exec, killall quickshell || quickshell
bind = $mainMod, D, exec, pgrep $menu && pkill $menu || $menu
bind = , PRINT, exec, $hyprregion
bind = $mainMod, PRINT, exec, $hyprwindow

# Ethernet
bind = $mainMod, T, exec, $terminal
bind = $mainMod, E, exec, $fileManager 
bind = $mainMod, B, exec, $browser

# Applications
bind = $mainMod, I, exec, $wal
bind = $mainMod, R, exec, $ref
bind = $mainMod, C, exec, $csp
bind = $mainMod, M, exec, $music
bind = $mainMod, N, exec, $editor
bind = $mainMod, W, exec, $notes

# Games
bind = CTRL SHIFT, 1, exec, $anime
bind = CTRL SHIFT, 2, exec, $honkers
bind = CTRL SHIFT, 3, exec, $sleepy
bind = CTRL SHIFT, 4, exec, heroic

# Layout 
bind = $mainMod, Q, togglespecialworkspace, magic 
bind = $mainMod SHIFT, Q, movetoworkspace, special:magic

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l 
bind = $mainMod, right, movefocus, r 
bind = $mainMod, up, movefocus, u 
bind = $mainMod, down, movefocus, d
bind = $mainMod, tab, cyclenext

bind = CTRL, left, movewindow, l
bind = CTRL, right, movewindow, r
bind = CTRL, up, movewindow, u
bind = CTRL, down, movewindow, d

bind = $mainMod CTRL, right, resizeactive, 60 0
bind = $mainMod CTRL, left, resizeactive, -60 0
bind = $mainMod CTRL, up, resizeactive, 0 -60
bind = $mainMod CTRL, down, resizeactive, 0 60

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1 
bind = $mainMod, 2, workspace, 2 
bind = $mainMod, 3, workspace, 3 
bind = $mainMod, 4, workspace, 4 
bind = $mainMod, 5, workspace, 5 
bind = $mainMod, 6, workspace, 6 
bind = $mainMod, 7, workspace, 7 
bind = $mainMod, 8, workspace, 8 
bind = $mainMod, 9, workspace, e-1
bind = $mainMod, 0, workspace, e+1


# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = CTRL, 1, movetoworkspace, 1 
bind = CTRL, 2, movetoworkspace, 2 
bind = CTRL, 3, movetoworkspace, 3 
bind = CTRL, 4, movetoworkspace, 4 
bind = CTRL, 5, movetoworkspace, 5 
bind = CTRL, 6, movetoworkspace, 6 
bind = CTRL, 7, movetoworkspace, 7 
bind = CTRL, 8, movetoworkspace, 8 
bind = CTRL, 9, movetoworkspace, e-1 
bind = CTRL, 0, movetoworkspace, e+1

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1 
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow 
bindm = $mainMod, mouse:273, resizewindow
# bindm = $mainMod, Z, movewindow
# bindm = $mainMod, C, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+ 
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- 
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle 
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle 
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+ 
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next 
bindl = , XF86AudioPause, exec, playerctl play-pause 
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

